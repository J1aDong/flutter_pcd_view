## 1. 非阻塞加载链路

- [x] 1.1 移除当前 viewer 打开路径对同步 FRB 解析接口的依赖，改为非阻塞解析调用
- [x] 1.2 为 PcdView 引入 request id 和 `idle/loading/preparing/ready/error` 请求状态管理
- [x] 1.3 实现 viewer 返回或切换文件时对陈旧结果的取消/忽略逻辑
- [x] 1.4 分离解析后点数据与渲染对象状态，确保 display-only 配置更新不触发重新解析

## 2. 交互与界面对齐

- [x] 2.1 按参考项目补齐 viewer 画布上的拖拽旋转交互
- [x] 2.2 补齐捏合缩放与滚轮缩放交互，并保持缩放边界受控
- [x] 2.3 确保 viewer 层手势与点大小等屏幕控制区可以同时工作
- [x] 2.4 保留并回传最新相机/控制器状态，验证返回后可复用

## 3. 诊断日志与大文件验证

- [x] 3.1 为 parse / prepare / navigation cancel-stale 三段关键路径增加统一结构化日志
- [x] 3.2 为大文件加载中的 loading/error 状态补充用户可见反馈
- [ ] 3.3 使用 `pointcloud_map.pcd` 验证 Android 真机上的打开、缩放旋转、返回路径是否仍卡顿
- [ ] 3.4 复测点大小调整、文件切换和多文件播放，确认不会触发新的明显阻塞
